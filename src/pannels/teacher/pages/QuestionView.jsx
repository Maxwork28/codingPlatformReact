import QuestionPreview from '../components/QuestionPreview';

/** Teacher question view uses the same preview + test-solution flow as admin. */
const QuestionView = () => <QuestionPreview />;

export default QuestionView;
